# TheoMedia (UK / EU) — Stripe (add when enabling card checkout)
# Get keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_xxxxxxxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxx
# Required in production for signed webhooks (endpoint: /api/stripe/webhook)
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxx

# Optional: flip online checkout in code after Stripe session + webhook are implemented
# (also set site.payments.onlineCheckoutEnabled = true in src/config/site.ts)
